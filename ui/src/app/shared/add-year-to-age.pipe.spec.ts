import { AddYearToAgePipe } from './add-year-to-age.pipe';

describe('AddYearToAgePipe', () => {
  it('create an instance', () => {
    const pipe = new AddYearToAgePipe();
    expect(pipe).toBeTruthy();
  });
});
